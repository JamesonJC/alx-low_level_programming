#include <stdio.h>
/**
 *main - main begins the program executionn
 *
 * Return: Program execution succesful
 */
int main(void)
{
	int n;

	for (n = 0; n < 10 ; n++)
		putchar('0' + n);
	putchar('\n');

	return (0);
}
